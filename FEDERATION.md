# 🌐 Freedom Hub Federation

Децентрализованная федеративная сеть Freedom Hub позволяет любому пользователю запустить свою ноду (сервер) и предоставлять сервисы платформы, получая доход, а создатель протокола (вы) получает **2% комиссию** со всех доходов нод.

## 📋 Содержание

- [Архитектура](#архитектура)
- [Как это работает](#как-это-работает)
- [Быстрый старт ноды](#быстрый-старт-ноды)
- [Требования к серверу](#требования-к-серверу)
- [Настройка ноды](#настройка-ноды)
- [Монетизация](#монетизация)
- [Протокол и комиссия 2%](#протокол-и-комиссия-2)

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                    Freedom Hub Protocol                         │
│                    (Super Admin: zametkikostik)                 │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │   Node 1    │  │   Node 2    │  │   Node N    │            │
│  │ cyber-rebel │  │ matrix.org  │  │  ...        │            │
│  │     2% ─────┼──┼──── 2% ─────┼──┼──── 2% ─────┤            │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │
│         │                │                │                     │
│         └────────────────┴────────────────┘                     │
│                          │                                      │
│                  ┌───────▼────────┐                             │
│                  │  Protocol Fee  │                             │
│                  │  Wallet (2%)   │                             │
│                  └────────────────┘                             │
└─────────────────────────────────────────────────────────────────┘
```

### Компоненты федерации:

1. **Protocol Layer** (центральный)
   - Смарт-контракты для распределения доходов
   - Реестр всех нод
   - Верификация и доверие
   - Super Admin панель

2. **Node Layer** (децентрализованный)
   - Независимые сервера пользователей
   - Локальные пользователи и данные
   - Автономное управление
   - Автоматическая отправка 2% протоколу

3. **User Layer**
   - Пользователи могут переключаться между нодами
   - Межнодовая коммуникация
   - Единый аккаунт для всей федерации

---

## ⚡ Как это работает

### Для владельца ноды:

1. **Установка**: Скачиваете Docker образ Freedom Hub Node
2. **Настройка**: Конфигурируете через панель управления
3. **Запуск**: Нода начинает принимать пользователей
4. **Доход**: Пользователи покупают Premium, используют AI сервисы
5. **Комиссия**: 2% автоматически отправляются протоколу
6. **Прибыль**: 98% остаётся владельцу ноды

### Для Super Admin (создатель):

- Получаете 2% от **каждой ноды** в федерации
- Контролируете протокол и обновления
- Верифицируете новые ноды
- Глобальная статистика и мониторинг

---

## 🚀 Быстрый старт ноды

### Вариант 1: Docker (рекомендуется)

```bash
# 1. Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 2. Создайте директорию для ноды
mkdir -p ~/freedom-node
cd ~/freedom-node

# 3. Скачайте конфигурацию
curl -O https://raw.githubusercontent.com/freedom-hub/federation/main/docker-compose.yml
curl -O https://raw.githubusercontent.com/freedom-hub/federation/main/.env.example

# 4. Настройте переменные окружения
cp .env.example .env
nano .env  # Отредактируйте под себя

# 5. Запустите ноду
docker-compose up -d

# 6. Проверьте статус
docker-compose ps

# 7. Откройте панель управления
# https://your-domain.com/admin
```

### Вариант 2: Ручная установка

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/freedom-hub/node.git
cd node

# 2. Установите зависимости
npm install

# 3. Настройте окружение
cp .env.example .env
nano .env

# 4. Инициализируйте базу данных
npm run db:migrate

# 5. Запустите сервер
npm run build
npm run start

# 6. Настройте reverse proxy (nginx)
sudo nano /etc/nginx/sites-available/freedom-node
# Конфигурация ниже ↓
```

---

## 🖥️ Требования к серверу

### Минимальные:

- **CPU**: 2 ядра
- **RAM**: 4 GB
- **Storage**: 50 GB SSD
- **Network**: 100 Mbps
- **OS**: Ubuntu 20.04+ / Debian 11+

### Рекомендуемые:

- **CPU**: 4 ядра
- **RAM**: 8 GB
- **Storage**: 100 GB NVMe SSD
- **Network**: 1 Gbps
- **OS**: Ubuntu 22.04 LTS

### Для больших нод (1000+ пользователей):

- **CPU**: 8+ ядер
- **RAM**: 16-32 GB
- **Storage**: 500 GB+ NVMe SSD
- **Network**: 10 Gbps
- **CDN**: Cloudflare / BunnyCDN

---

## ⚙️ Настройка ноды

### Файл `.env`:

```bash
# ─────────────────────────────────────────────────────────────
# NODE CONFIGURATION
# ─────────────────────────────────────────────────────────────
NODE_ID=auto-generate
NODE_NAME=My Freedom Node
NODE_DOMAIN=https://your-domain.com
NODE_ADMIN_EMAIL=admin@your-domain.com

# ─────────────────────────────────────────────────────────────
# PROTOCOL SETTINGS (2% commission)
# ─────────────────────────────────────────────────────────────
PROTOCOL_WALLET=0xYourWalletAddress
PROTOCOL_FEE_PERCENT=2
PROTOCOL_AUTO_SEND=true
PROTOCOL_SEND_INTERVAL=86400  # daily in seconds

# ─────────────────────────────────────────────────────────────
# DATABASE
# ─────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@localhost:5432/freedom_hub

# ─────────────────────────────────────────────────────────────
# REDIS (cache & queues)
# ─────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ─────────────────────────────────────────────────────────────
# STORAGE (IPFS for decentralized storage)
# ─────────────────────────────────────────────────────────────
IPFS_URL=http://localhost:5001
STORAGE_PATH=/var/lib/freedom-node/storage

# ─────────────────────────────────────────────────────────────
# API KEYS (for AI services)
# ─────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
ELEVENLABS_API_KEY=...

# ─────────────────────────────────────────────────────────────
# PAYMENT PROCESSING
# ─────────────────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_live_...
CRYPTO_WALLET_ADDRESS=0xYourWalletAddress
ENABLE_CRYPTO=true
ENABLE_FIAT=true

# ─────────────────────────────────────────────────────────────
# SECURITY
# ─────────────────────────────────────────────────────────────
JWT_SECRET=your-super-secret-jwt-key
SESSION_SECRET=your-session-secret
ENCRYPTION_KEY=your-32-byte-encryption-key

# ─────────────────────────────────────────────────────────────
# FEDERATION
# ─────────────────────────────────────────────────────────────
FEDERATION_ENABLED=true
FEDERATION_MODE=full  # full, light, or private
PEER_NODES=
ALLOWED_PEERS=
BLOCKED_PEERS=

# ─────────────────────────────────────────────────────────────
# MONITORING & ANALYTICS
# ─────────────────────────────────────────────────────────────
ENABLE_ANALYTICS=true
SENTRY_DSN=
TELEMETRY_ENABLED=true
```

### Nginx конфигурация:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # Redirect to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    # SSL certificates (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    
    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    
    # Main application
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
    
    # WebSocket for real-time features
    location /ws {
        proxy_pass http://localhost:3000/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
    
    # Static files
    location /static {
        alias /var/lib/freedom-node/public;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Health check
    location /health {
        proxy_pass http://localhost:3000/health;
        access_log off;
    }
}
```

---

## 💰 Монетизация

### Источники дохода ноды:

1. **Premium подписки**
   - Monthly: $4.99
   - Yearly: $49.99
   - Lifetime: $199.99

2. **AI сервисы**
   - AI Chat: $0.01/сообщение
   - AI Image: $0.05/изображение
   - AI Voice: $0.02/минута

3. **Хранение данных**
   - Cloud Storage: $0.10/GB/месяц
   - IPFS Pinning: $0.05/GB/месяц

4. **Дополнительно**
   - Кастомные домены: $5/месяц
   - Приоритетная поддержка: $10/месяц
   - API доступ: $20/месяц

### Распределение доходов:

```
┌─────────────────────────────────────┐
│         Node Revenue: $1000         │
└──────────────┬──────────────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
┌───▼────┐         ┌─────▼────┐
│  2%    │         │   98%    │
│ $20    │         │  $980    │
│        │         │          │
│Protocol│         │   Node   │
│ Wallet │         │  Owner   │
└────────┘         └──────────┘
```

---

## 🔗 Протокол и комиссия 2%

### Автоматические выплаты:

Нода автоматически отправляет 2% от всех доходов на кошелёк протокола:

```javascript
// Пример смарт-контракта для распределения доходов
contract FreedomHubProtocol {
    address public superAdmin;  // zametkikostik
    uint256 public protocolFee = 2;  // 2%
    
    mapping(address => uint256) public nodeEarnings;
    
    event FeeCollected(address indexed node, uint256 amount);
    
    function distributeRevenue(address node, uint256 totalRevenue) external {
        require(msg.sender == node, "Only node can call");
        
        uint256 protocolShare = (totalRevenue * protocolFee) / 100;
        uint256 nodeShare = totalRevenue - protocolShare;
        
        // Send 2% to protocol
        payable(superAdmin).transfer(protocolShare);
        
        // Node keeps 98%
        payable(node).transfer(nodeShare);
        
        emit FeeCollected(node, protocolShare);
    }
}
```

### Регистрация ноды в федерации:

1. **Подача заявки**:
   ```bash
   curl -X POST https://freedom-hub.app/api/federation/nodes \
     -H "Content-Type: application/json" \
     -d '{
       "nodeDomain": "your-domain.com",
       "adminEmail": "admin@your-domain.com",
       "nodeName": "My Freedom Node"
     }'
   ```

2. **Верификация Super Admin**:
   - Проверка через панель `/admin/verification`
   - Подтверждение соответствия требованиям
   - Добавление в реестр нод

3. **Настройка Federation**:
   ```bash
   # После одобрения получите NODE_ID и API ключ
   export NODE_ID=your-node-id
   export FEDERATION_API_KEY=your-api-key
   ```

---

## 📊 Мониторинг ноды

### Панель управления:

- **https://your-domain.com/admin** — админ панель
- **https://your-domain.com/admin/sip** — SIP настройки
- **https://your-domain.com/admin/api** — API ключи
- **https://your-domain.com/federation** — статус федерации

### Метрики для отслеживания:

- Пользователей онлайн
- Активных сессий
- Доход за период
- Отправлено комиссии (2%)
- Использование ресурсов
- Состояние базы данных

---

## 🛡️ Безопасность

### Обязательные меры:

1. **SSL/TLS**: Все соединения только по HTTPS
2. **Firewall**: Откройте только необходимые порты (80, 443)
3. **Fail2Ban**: Защита от брутфорса
4. **Regular Updates**: Обновляйте систему и зависимости
5. **Backups**: Ежедневные бэкапы базы данных
6. **Encryption**: Шифрование чувствительных данных

---

## 📞 Поддержка

### Для владельцев нод:

- **Документация**: https://docs.freedom-hub.app
- **Discord**: https://discord.gg/freedom-hub
- **Telegram**: https://t.me/freedom_hub_nodes
- **Email**: nodes@freedom-hub.app

### Для Super Admin:

- **Панель**: https://freedom-hub.app/super-admin
- **Мониторинг нод**: https://freedom-hub.app/admin/nodes
- **Комиссии**: https://freedom-hub.app/admin/nodes/revenue

---

## 📈 Roadmap федерации

- [x] Базовая архитектура
- [x] Панель Super Admin
- [x] Настройки SIP VoIP
- [ ] Docker образ ноды
- [ ] Автоматическая регистрация нод
- [ ] Смарт-контракт для 2% комиссии
- [ ] Межнодовая коммуникация
- [ ] Децентрализованное хранение (IPFS)
- [ ] P2P звонки между нодами
- [ ] Федеративная лента контента

---

**Freedom Hub Federation** — будущее децентрализованных коммуникаций! 🚀
