# 🚀 БЫСТРЫЙ СТАРТ

## 1️⃣ Установка зависимостей

Откройте терминал в папке проекта:

```bash
cd /home/kostik/freedom-hub
npm install
```

**Время установки:** ~2-5 минут

---

## 2️⃣ Настройка переменных окружения

Создайте файл `.env.local`:

```bash
cp .env.example .env.local
```

Откройте `.env.local` и заполните минимальные значения:

```bash
# Supabase (получите в https://supabase.com)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# WalletConnect (получите в https://cloud.walletconnect.com)
VITE_WALLET_CONNECT_PROJECT_ID=your-project-id
```

> ⚠️ **Без этих переменных проект не запустится!**

---

## 3️⃣ Запуск dev-сервера

```bash
npm run dev
```

**Готово!** Вы увидите:

```
VITE v5.0.12  ready in 2500 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
➜  press h + enter to show help
```

---

## 4️⃣ Откройте в Firefox

Скопируйте и вставьте в адресную строку:

```
http://localhost:5173
```

Или нажмите **`o`** в терминале — браузер откроется автоматически!

---

## ✅ Проверка работы

1. **Откройте** `http://localhost:5173` в Firefox
2. **Должны увидеть:**
   - Страницу авторизации (`/auth`)
   - Киберпанк дизайн (тёмный фон, неоновые акценты)
   - Кнопки "Вход" и "Подключить кошелёк"

3. **Проверьте HMR:**
   - Откройте `src/App.tsx`
   - Измените текст (например, заголовок)
   - Сохраните (`Ctrl+S`)
   - Страница обновится **автоматически**! ✨

---

## 🐛 Если что-то не работает

| Проблема | Решение |
|----------|---------|
| `npm install` долго работает | Удалите `node_modules` и `package-lock.json`, затем `npm install` заново |
| Порт 5173 занят | Запустите `npm run dev -- --port 3000` |
| Переменные не работают | Перезапустите `npm run dev` после изменения `.env.local` |
| HMR не работает | Проверьте, что файл сохранён (`Ctrl+S`) |

---

## 📞 Ссылки

- **Локальный сервер:** http://localhost:5173
- **GitHub Actions:** https://github.com/your-username/freedom-hub/actions

---

**Готово к разработке!** 🎉
